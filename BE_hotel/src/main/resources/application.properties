spring.application.name=BE_hotel

# Active profile: use 'mysql' to connect to MySQL via XAMPP
spring.profiles.active=mysql

# JPA Configuration (common for all profiles)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=false

# VNPay (sandbox) configuration - demo/test keys provided by user
# Replace these with production keys when going live
vnpay.tmn-code=BQCVOBA7
vnpay.hash-secret=PN0N8IKYGUR6J9PYXCBWBKQEWRQM7AJJ
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.return-url=http://localhost:3000/payment-result

# Mail configuration (update with real SMTP credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyenthaisonthuy@gmail.com
spring.mail.password=ndah eyeu eunf ouic
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=no-reply@yourdomain.com

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Root folder for storing uploaded files (relative to app working dir by default)
app.upload.dir=uploads
app.upload.max-size=5242880
app.upload.max-width=1920
app.upload.max-height=1080

# Database connection pool optimization (HikariCP)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000